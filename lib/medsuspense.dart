import 'package:flutter/material.dart';
import 'medsuspense_app.dart';

class MedSuspensePage extends StatelessWidget {
  const MedSuspensePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const MedSuspenseApp();
  }
}